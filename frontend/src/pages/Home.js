import { useContext } from "react";
import { AuthContext } from "../context/AuthContext";
import Button from "../components/Button";

const Home = () => {

    return (
        <div className="max-w-md mx-auto mt-10">
            <h2 className="text-2xl font-bold">Notre site...</h2>
        </div>
    );
};

export default Home;
